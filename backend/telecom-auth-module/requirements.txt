# --- Core framework ---
# Pinned: FastAPI 0.137.x has a nested-router flattening regression; 0.115.x is stable.
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0

# --- Validation / settings ---
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0

# --- Security ---
pyjwt>=2.10.0
passlib[bcrypt]>=1.7.4
# Pinned: passlib is incompatible with bcrypt>=4.1 (reads __about__.__version__).
bcrypt==4.0.1

# --- Test (optional) ---
httpx>=0.28.0
aiosqlite>=0.20.0
pytest>=8.3.0
pytest-asyncio>=0.25.0
python-multipart>=0.0.9